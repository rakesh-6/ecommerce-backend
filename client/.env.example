## Client environment (copy to `client/.env`)
## Vite only exposes vars prefixed with VITE_

# Backend base URL (Render/Railway/etc in production)
VITE_API_URL=http://localhost:5000

# Razorpay public key id (safe to expose)
VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id
