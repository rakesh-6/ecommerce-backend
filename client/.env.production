# ⚠️ PRODUCTION ENVIRONMENT CONFIG - CLIENT
# When deployed on Render as a single service, the API is on the same origin.
# Use an empty string so axios calls go to the same host.

# 🚀 BACKEND API URL (same origin when frontend is served by the backend)
VITE_API_URL=

# 💳 RAZORPAY PUBLIC KEY (SAFE TO EXPOSE)
# Get from: https://dashboard.razorpay.com/app/keys
VITE_RAZORPAY_KEY_ID=rzp_test_placeholder
